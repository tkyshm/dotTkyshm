conky.config = {
    short_units=true,
    background=true,
    out_to_console=true,
    out_to_x=false,
    -- Update interval in seconds
    update_interval=1
    --update_interval_on_battery 5
}

conky.text =[[
S${cpu cpu0} ${execi 1 ./panel_temperature}${execi 1 ./battery} ${execi 1 ./vols} ${execi 1 ./wifi} ^fg(\#239482)${time %Y/%m/%d-%H:%M:%S}
]]
-- S${execi 1 ./panel_temperature} ${execi 1 ./wifi} ${wireless_essid wlan0} ${execi 1 ./battery}${battery_percent} ${time %Y/%m/%d-%H:%M:%S}
